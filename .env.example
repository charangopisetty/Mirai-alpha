# Resend API key (Dashboard → API Keys). Never expose in client code.
RESEND_API_KEY=re_VJJ67sXK_GCPgJaj82FeDeMHHZq3snNwD

# Optional. If unset, the waitlist uses event "email.added" (same as Resend curl POST /events/send).
# RESEND_WAITLIST_EVENT=email.added
